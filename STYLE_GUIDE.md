# راهنمای سبک نگارش و اصطلاحات

این پروژه برای خواننده‌ای نوشته می‌شود که ممکن است هیچ پیش‌زمینه‌ای در distributed systems نداشته باشد. هدف، ساده‌کردن مسیر فهم است؛ نه ساده‌سازی بیش از اندازهٔ مفهوم.

## قاعدهٔ هر مبحث

هر اصطلاح یا روش مهم باید در بخش جداگانه‌ای با این ترتیب معرفی شود:

1. **چیست؟** تعریف یک‌جمله‌ای با زبان ساده.
2. **چرا لازم است؟** مشکلی که حل می‌کند.
3. **چطور کار می‌کند؟** چند گام روشن یا یک نمودار کوچک.
4. **مثال روزمره یا فنی:** یک سناریوی مستقل.
5. **خطر یا trade-off:** چه چیزی را بهتر و چه چیزی را سخت‌تر می‌کند.
6. **اشتباه رایج:** برداشتی که ممکن است خواننده را گمراه کند.

از هر پاراگراف فقط یک ایدهٔ اصلی انتظار می‌رود. واژهٔ جدید نباید بدون تعریف وارد متن شود و هر مخفف باید در اولین استفاده باز شود.

## زبان برای خوانندهٔ مبتدی

- جمله‌ها کوتاه و مستقیم باشند.
- قبل از نام ابزار، مسئله توضیح داده شود.
- از مثال‌های ملموس مانند سفارش، پرداخت، پیام، موجودی و اعلان استفاده شود.
- بعد از تعریف، یک مثال مخالف یا failure هم گفته شود.
- فرق میان «داده»، «event»، «پیام»، «وضعیت» و «منبع حقیقت» صریح نوشته شود.
- جدول و نمودار برای مقایسه استفاده شود، اما هیچ نموداری جای توضیح متن را نگیرد.
- سؤال‌های مرور با پاسخ کوتاه یا مسیر فکرکردن همراه باشند.

## واژه‌هایی که ترجمه نمی‌شوند

این واژه‌ها در متن و عنوان‌ها به همان شکل انگلیسی و داخل code span می‌آیند؛ سپس با فارسی ساده توضیح داده می‌شوند:

`idempotency`, `idempotent`, `document`, `relational`, `database`, `cache`, `API`, `REST`, `RPC`, `schema`, `outbox`, `inbox`, `saga`, `retry`, `timeout`, `backoff`, `jitter`, `CDC`, `event sourcing`, `Kafka`, `SQL`, `NoSQL`, `WAL`, `B-tree`, `LSM-tree`, `MapReduce`, `OLTP`, `OLAP`, `atomicity`, `quorum`, `leader`, `follower`, `partition`, `partitioning`, `sharding`, `rebalancing`, `replication`, `normalization`, `denormalization`, `consistency`, `consensus`, `linearizability`, `compatibility`, `reliability`, `scalability`, `maintainability`, `operability`, `simplicity`, `evolvability`.

در صورت نیاز می‌توان کنار واژه توضیح فارسی آورد؛ برای نمونه: `idempotency` یعنی «تکرار امن یک درخواست بدون چندبرابرشدن اثر نهایی». خود واژه به «هم‌توان» یا معادل‌های مبهم ترجمه نمی‌شود.

### سه اصطلاحی که نباید با هم قاطی شوند

- `normalization`: کم‌کردن duplication و نگه‌داشتن حقیقت data در یک محل.
- `denormalization`: duplication کنترل‌شده برای سریع‌ترکردن read.
- `rebalancing`: جابه‌جایی مالکیت `partition`ها بعد از اضافه/حذف node یا تغییر load.

`rebalancing` هیچ ارتباطی با `normalization` یا `denormalization` ندارد. در متن فارسی فقط توضیح می‌دهیم؛ خود سه واژه را ترجمه نمی‌کنیم.

## الگوی تعریف مستقل

```markdown
### `idempotency`

**چیست؟** ...

**مثال ساده:** ...

**خطر یا trade-off:** ...

**اشتباه رایج:** ...
```

## مرجع سبک

فایل فارسی `Software_Engineer_Systems_Handbook_FA.pdf` فقط به‌عنوان مرجع لحن آموزشی، ترتیب تعریف‌ها و نوع مثال‌ها بررسی شده است. متن آن در این پروژه کپی نمی‌شود و مثال‌ها و توضیح‌های این مخزن مستقل نوشته می‌شوند.
